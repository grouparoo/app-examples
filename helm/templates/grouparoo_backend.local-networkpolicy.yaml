apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: grouparoo-backend.local
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/grouparoo-backend.local: "true"
  podSelector:
    matchLabels:
      io.kompose.network/grouparoo-backend.local: "true"
