#!/usr/bin/env bash

# the `kompose` package is required to already be installed and in your path - https://kubernetes.io/docs/tasks/configure-pod-container/translate-compose-kubernetes/

cd "$(dirname "$0")"
cd ..

rm -rf helm/*.yaml

kompose convert --file=docker-compose.published.yml --build=local --out=helm --chart
