apiVersion: v1
kind: Service
metadata:
  name: {{ include "grouparoo-example-app.fullname" . }}
  labels:
    {{- include "grouparoo-example-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "grouparoo-example-app.selectorLabels" . | nindent 4 }}
